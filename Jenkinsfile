pipeline {

    agent any

    stages {
        stage('Build') {
            steps {
                echo 'app build'
            }
        }  
         stage('test') {
            steps {
                echo 'app test'
            }
        }  
         stage('deploy') {
            steps {
                echo 'app deploy'
            }
        }  
        
    }
}
